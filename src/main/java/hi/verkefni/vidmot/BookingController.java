package hi.verkefni.vidmot;
// BookingController
// + createBooking(customer: Customer,flight: Flight, seats: List<Seat>) : Booking
// + cancelBooking(bookingId: String): boolean
// + updateBooking(bookingId: String, newSeats: List<Sear>): Booking
// + viewBooking(bookingId: String): Booking

// BookingDB
// + selectById(bookingId: String) : Booking
// + insert(booking: Booking) : void
// + update(booking: Booking) : void
// + delete(bookingId: String) : void
public class BookingController {
}
